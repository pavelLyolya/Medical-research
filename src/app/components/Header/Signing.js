import React from 'react';
import Button from './Button';

const Signing = () => (
    <div className='signing'>
        <Button text='Sign in' />
        <span>or</span>
        <Button text='Sign up' />
    </div>
);

export default Signing;
